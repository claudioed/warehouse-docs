---
id: allocation
title: "Allocation"
description: "Allocation"
custom_edit_url: null
---



Automatic allocation of stock per line through inventory-storage, folded into `POST /orders` and `POST /orders/{id}/retry-allocation`. A `409` from that Supplier backorders a single line (a business fact); any other failure fails the whole allocation pass (fail-closed on ambiguity). A backordered line returns to `Allocated` only via retry-allocation, which also re-attempts release for any lines it newly clears.




```mdx-code-block
import DocCardList from '@theme/DocCardList';
import {useCurrentSidebarCategory} from '@docusaurus/theme-common';

<DocCardList items={useCurrentSidebarCategory().items}/>
```
      