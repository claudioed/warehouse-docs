---
id: layout
title: "Layout"
description: "Layout"
custom_edit_url: null
---



The readable, drawable projections: a Site's full nested structure (optionally rendered server-side as an SVG floor plan) and a Zone's 2D grid. These are projections assembled across the aggregates, never separately stored state.




```mdx-code-block
import DocCardList from '@theme/DocCardList';
import {useCurrentSidebarCategory} from '@docusaurus/theme-common';

<DocCardList items={useCurrentSidebarCategory().items}/>
```
      