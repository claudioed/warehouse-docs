---
id: reservations
title: "Reservations"
description: "Reservations"
custom_edit_url: null
---



Revocable bindings of usable stock to demand. A Reservation can be revoked (returning its quantity to usable) or confirmed as picked (consuming it) — it never permanently binds to one physical bin, so a failed pick can be re-satisfied elsewhere.




```mdx-code-block
import DocCardList from '@theme/DocCardList';
import {useCurrentSidebarCategory} from '@docusaurus/theme-common';

<DocCardList items={useCurrentSidebarCategory().items}/>
```
      